<UserControl x:Class="Logistics_Software.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Logistics_Software.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.DataContext>
        <vm:UsersViewModel/>
    </UserControl.DataContext>

    <Grid Background="#FFFFFF">
        <StackPanel Margin="20">
            <TextBlock Text="Пользователи" FontSize="20" FontWeight="Bold"/>

            <ListBox ItemsSource="{Binding Users}" Height="200" Margin="0,10"/>

            <TextBox x:Name="UserNameBox" PlaceholderText="Введите имя" Margin="0,5"/>

            <Button Content="Добавить" Command="{Binding AddUserCommand}" CommandParameter="{Binding Text, ElementName=UserNameBox}" Background="#FF6B35" Foreground="White" Height="40"/>
        </StackPanel>
    </Grid>
</UserControl>
