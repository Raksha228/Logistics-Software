<UserControl x:Class="Logistics_Software.Views.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Logistics_Software.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.DataContext>
        <vm:NavigationViewModel/>
    </UserControl.DataContext>

    <StackPanel Orientation="Vertical" Background="#333333" Width="200">
        <Button Content="Заказы" Command="{Binding NavigateToCommand}" CommandParameter="Orders" Background="Transparent" Foreground="White"/>
        <Button Content="Пользователи" Command="{Binding NavigateToCommand}" CommandParameter="Users" Background="Transparent" Foreground="White"/>
        <Button Content="Выход" Command="{Binding NavigateToCommand}" CommandParameter="Login" Background="Transparent" Foreground="White"/>
    </StackPanel>
</UserControl>
